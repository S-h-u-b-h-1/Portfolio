{
  "name": "shubhaang-kataruka-portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Shubhaang Kataruka's portfolio website.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace backend",
    "prisma:generate": "npm run prisma:generate --workspace backend",
    "prisma:deploy": "npm run prisma:deploy --workspace backend",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
